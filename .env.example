# Copy this file to .env.local and fill in your key.
# Powers the site AI assistant (src/app/api/chat). Without it, the assistant
# shows a friendly "offline" message and directs visitors to phone/email.
# Get a key at https://console.anthropic.com/ (Settings → API Keys).
ANTHROPIC_API_KEY=sk-ant-...
